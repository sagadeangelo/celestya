app = "celestya-backend"
primary_region = "dfw"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_start_machines = true
  auto_stop_machines = false
  min_machines_running = 1
  processes = ["app"]

[checks]
  [checks.http]
    type = "http"
    port = 8080
    method = "GET"
    path = "/openapi.json"
    interval = "30s"
    timeout = "10s"
    grace_period = "20s"

[[mounts]]
  source = "celestya_data"
  destination = "/data"

[vm]
  size = "shared-cpu-1x"
  memory = "1024mb"
